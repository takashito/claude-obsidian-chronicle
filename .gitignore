.DS_Store
*.log
*.lock
.env
# Local Claude Code config, project-local plugin config, and private maintainer skills (per-machine; don't commit)
.claude/
# Internal materials (promotion strategy, drafts, etc.; not for the public repo)
.internal/
